# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Database
DATABASE_SSL=0
DATABASE_ENGINE=postgres

# -- Local --#
#DATABASE_PASSWORD=postgres
#DATABASE_HOST=localhost
# DATABASE_PORT=5435

# -- H2 IAM --#
DATABASE_HOST=infrastack-prismadb20db0e1d-z0gpzngdv9rn.cw5b8fhlm28z.us-east-1.rds.amazonaws.com
DATABASE_NAME=prismaDb
DATABASE_USER=postgres
DATABASE_PASSWORD=onGM8,nwP0xr2ned1djKmXqlR_h7gx
DATABASE_PORT=5432

DATABASE_URL="${DATABASE_ENGINE}://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?schema=${DATABASE_NAME}&connection_limit=1"
